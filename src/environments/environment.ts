export const environment = {
    production: true,
    apiKey: '',
    apiUrl: 'https://www.googleapis.com/youtube/v3/videos?part=snippet,contentDetails&fields=item(id,snippet(title,thumbnails(default)),contentDetails(duration))',
};
